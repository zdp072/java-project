server:
  port: 12011

zlt:
  nacos:
    server-addr: 127.0.0.1:8848

spring:
  application:
    name: txlcn-demo-spring-service-a
  cloud:
    nacos:
      discovery:
        server-addr: ${zlt.nacos.server-addr}